{
  "name": "pot-code-identifier-plugin",
  "version": "0.2.0",
  "private": true,
  "description": "Offline Pot plugin for programming-aware identifier translation and general English dictionary lookup.",
  "scripts": {
    "build:runtime": "python scripts/build_runtime.py",
    "test": "python scripts/build_runtime.py && node --test tests/*.test.cjs",
    "test:dictionary": "python scripts/test_dictionary_build.py"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "GPL-3.0-only"
}
