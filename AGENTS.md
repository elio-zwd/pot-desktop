# Pot 社区维护版长期协作规则

## 1. 项目身份与仓库

- 上游仓库：`https://github.com/pot-app/pot-desktop`
- 社区维护仓库：`https://github.com/elio-zwd/pot-desktop`
- 对外名称：`Pot 社区维护版`
- 项目性质：非官方社区维护版，不代表官方 Pot 或 Manggo
- 许可证：继续遵循 GPL-3.0，必须保留原作者、原项目和许可证声明
- 初期正式支持平台：Windows x64

任何文档、界面、安装包或发布说明都不得让用户误认为本仓库是官方 Pot、官方继任项目或 Manggo 的组成部分。

## 2. 分支职责

- `master`：只跟踪历史上游或必要的上游同步，不承载社区定制开发。
- `custom/main`：社区维护版稳定主线，只接收已验收的 PR。
- `feat/*`：独立功能。
- `fix/*`：独立缺陷修复。
- `chore/*`：治理、构建、发布和维护基础设施。
- `port/upstream-pr-<number>-<topic>`：上游 PR 的独立审计与移植。

禁止直接在 `master` 或 `custom/main` 开发。所有开发分支必须从当时最新的 `custom/main` 明确 SHA 创建，并先进入 Draft PR。未经仓库所有者明确授权，不得转 Ready、合并、启用自动合并、强推或删除远端分支。

## 3. 必读顺序

每次接手任务必须按以下顺序读取：

1. `README.md`
2. `AGENTS.md`
3. 当前分支对应的 `plans/` 文档
4. 当前分支对应的 `tasks/` 文档
5. 当前分支对应的 `handoffs/` 文档
6. 与任务直接相关的决策文档
7. 许可证、真实源码、配置、测试与工作流

不得根据旧对话、旧 PR 或已经结束的功能分支猜测当前实现。

## 4. 语言与提交规范

- 报告、分析、文档和代码注释统一使用中文。
- Git Commit 必须采用 `英文 Tag: 中文描述`。
- 示例：`fix: 隔离官方自动更新通道`
- 示例：`docs: 添加上游 PR 移植政策`
- 示例：`ci: 添加 custom/main 轻量门禁`
- 不允许无意义提交、全仓库自动格式化或混入无关修改。

## 5. 稳定兼容边界

当前稳定主线已经包含插件设置 Schema V2 与插件结果 Schema V2。后续变更必须继续保证：

- 旧版插件 `needs` 配置可用；
- 旧版字符串结果可用；
- 旧版 `explanations/associations` 等对象结果可用；
- 设置分组、密码遮罩、多行输入、条件显示和高级设置折叠行为不被破坏；
- 六类受控结果区块、统一复制文本和历史记录纯文本不被破坏；
- 插件不得注入 React 组件、任意 HTML、JavaScript、自定义 CSS 或任意样式类名；
- 不使用 `dangerouslySetInnerHTML` 渲染插件提供的新 Schema 内容。

除非任务明确要求并经过独立设计，不得重写 Schema V2 或改变其兼容语义。

## 6. 技术与依赖边界

- 当前基础为 Pot 3.0.7、React 18、NextUI 2、Tauri 1.x。
- 不在普通功能或维护 PR 中迁移 Tauri 2。
- 不整体升级 React、NextUI、Rust/Tauri 或依赖栈。
- 不增加运行时第三方依赖，除非先说明必要性并获得确认。
- 不顺手修改与当前任务无关的 `Cargo.lock`、格式、图标或全局 UI。
- 已知的 Windows `tauri_plugin_single_instance` 空指针问题必须通过独立 `fix/` 或 `port/` 分支处理。

## 7. 上游 PR 审计与移植

移植上游 PR 前必须：

1. 读取完整 diff、讨论、CI、依赖和相关 Issue；
2. 判断安全性、稳定性、用户相关性、修改规模和长期维护成本；
3. 将不相关问题拆成独立分支；
4. 在最新 `custom/main` 上重新应用并验证；
5. 优先保留原作者 commit、Authorship 或标准 `Co-Authored-By`；
6. 在 `UPSTREAM_PORTS.md` 记录接受、拒绝、等待或替代实现结论。

不得因为上游 PR 已关闭、长期未响应或看似简单就默认其正确，也不得整体吸收跨多个模块的大杂烩 PR。

## 8. 发布、更新与敏感信息

- 自有更新通道完成前，社区维护版必须关闭或隔离官方自动更新。
- 不得使用上游签名密钥、证书、更新端点或发布凭据。
- 不得把更新端点改成不存在的占位服务。
- 不得提交 API Key、访问令牌、证书、签名私钥、用户配置、数据库、日志隐私文本或真实翻译内容。
- 版本写入、安装包品牌化、自有签名和自有更新服务必须通过独立发布 PR 完成。
- 未经明确授权，不得发布安装包、GitHub Release 或更新清单。

## 9. 测试与验收

每个分支至少执行与修改范围匹配的定向门禁：

- 相关纯函数或 Schema 测试；
- 必要的旧版兼容测试；
- `pnpm build`；
- 新增 YAML、JSON 和 Markdown 的基础有效性检查；
- `git diff --check`；
- 构建后受跟踪工作区未发生意外变化；
- 工作区干净检查。

若没有修改桌面运行时 UI 或样式，不重复完整人工 UI 验收。避免为了测试引入大型测试框架，优先使用 Node 内置测试和现有工具。

## 10. AI 协作边界

- 远端 AI：读取仓库、规划、编写代码与文档、执行轻量测试、提交并创建 Draft PR。
- 本地 AI：拉取指定远端 SHA，构建和只读验收。
- 本地 AI 不修改代码、不提交、不推送、不合并。
- 远端 AI 完成后必须提供包含仓库链接、分支和精确 HEAD 的本地只读验收 Prompt。
