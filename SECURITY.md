# Pot 社区维护版安全政策

## 支持范围

Pot 社区维护版是 `elio-zwd/pot-desktop` 的非官方社区维护分支，不代表官方 Pot 或 Manggo。

当前安全维护范围：

- 稳定分支：`custom/main`；
- 正在审查的、以 `custom/main` 为 Base 的维护版 Draft PR；
- 初期正式支持平台：Windows x64；
- 插件平台、凭据处理、网络请求、更新器、启动与用户数据完整性。

`master` 只用于跟踪历史上游，不作为社区维护版的安全修复交付分支。

## 如何报告

请优先通过本仓库的私密安全报告功能提交漏洞。若该功能不可用，请创建一个不包含敏感细节的普通 Issue，仅说明需要私下联系维护者，不要公开漏洞利用步骤或秘密数据。

本维护版问题应报告到：

`https://github.com/elio-zwd/pot-desktop`

请勿以本维护版问题向官方 Pot、原作者或 Manggo 要求支持。

## 不得公开提交的内容

禁止在公开 Issue、PR、日志或附件中粘贴：

- API Key、访问令牌、Cookie、账号密码；
- 证书、签名私钥、更新签名材料；
- 用户配置目录、数据库或完整备份；
- 未脱敏的翻译文本、剪贴板内容、截图或 OCR 结果；
- 可直接利用的漏洞细节或真实用户数据；
- 包含个人路径、账号标识或服务凭据的完整日志。

若秘密已经公开，应立即在对应服务端撤销或轮换；删除 GitHub 内容不能替代凭据轮换。

## 高优先级问题

以下问题按高优先级评估：

- API Key、令牌、Cookie 或签名材料泄露；
- 本应使用 HTTPS 的敏感请求使用明文传输；
- 插件或外部输入导致任意代码执行；
- 不受控 HTML、JavaScript、CSS 或组件注入；
- 更新器可安装错误来源或未授权发布物；
- 用户数据库、配置或历史记录被破坏、覆盖或外泄；
- 无需明确授权即可访问或发送用户隐私内容。

## 复现要求

请提供不依赖真实秘密数据的最小复现：

- 维护版版本、分支或精确 commit SHA；
- 操作系统和架构；
- 最小步骤、预期结果和实际结果；
- 脱敏日志或测试夹具；
- 使用 `example-key`、本地 mock 服务或无效测试凭据；
- 影响范围、是否可重复和已知缓解方式。

不要为了证明漏洞而访问他人账号、扩大数据暴露或对公共服务进行破坏性测试。

## 更新、签名与发布

在自有更新通道完成前，社区维护版保持自动更新关闭，不使用上游 updater 公钥、端点、证书或签名密钥。

自有签名、更新服务器和发布流程只能在独立分支中实现，并经过凭据隔离、最小权限和回滚审计。任何真实私钥都不得提交到仓库。

## 响应说明

本项目由社区维护，暂不承诺固定响应 SLA。报告会根据影响范围、可利用性、用户数据风险、维护范围和可复现程度排序。

维护者可能要求补充脱敏信息，也可能把不属于本维护版的问题转回插件作者、第三方服务或上游项目。
